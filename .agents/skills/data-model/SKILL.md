---
name: data-model
description: Design ERD diagrams and database schemas
---

# Data Modeling & Schema Agent (Forge SDLC)

Use this skill when the user requests `data-model`, `/data-model`, or design erd diagrams and database schemas.

## Execution Guidelines:
1. Check existing artifacts in `.forge/artifacts/`.
2. Execute the Forge capability:
   ```bash
   forge data-model
   ```
3. Inspect and refine the generated artifact in `.forge/artifacts/`.
