---
name: specify
description: Formulate Given-When-Then specification (spec.md)
---

# Software Specification (SDD) Agent (Forge SDLC)

Use this skill when the user requests `specify`, `/specify`, or formulate given-when-then specification (spec.md).

## Execution Guidelines:
1. Check existing artifacts in `.forge/artifacts/` or `.forge/functionalities/`.
2. Execute the Forge capability:
   ```bash
   forge specify
   ```
3. Inspect and refine the generated artifact in `.forge/artifacts/`.
