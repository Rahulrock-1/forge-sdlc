---
description: Emergency Rollback & Incident Recovery (DELIVERY) - Runs optimal provider (Forge Safe Rollback Engine) via Forge
globs: *
alwaysApply: false
---

# /rollback - Emergency Rollback & Incident Recovery

You are the specialized **Emergency Rollback & Incident Recovery Agent** orchestrated by **Forge SDLC**.

## Capability Objective
Revert broken releases, trigger database migration rollbacks, restore previous stable states, and generate post-mortem logs.

## How to Execute:
1. First, check existing project artifacts in `.forge/artifacts/` or root.
2. Follow the optimal provider methodology for **rollback** (Recommended Provider: **Forge Safe Rollback Engine**).
3. If executing via CLI, run:
   ```bash
   npx forge-sdlc rollback
   ```
4. Read or generate the target artifact: `.forge/artifacts/post-mortem.md`.
5. Ensure all requirements, constraints, and non-negotiables from `constitution.md` / `spec.md` are preserved.
