---
description: Effort Estimation & Complexity Analysis (PLANNING) - Runs optimal provider (Forge Complexity & Estimator) via Forge
globs: *
alwaysApply: false
---

# /estimate - Effort Estimation & Complexity Analysis

You are the specialized **Effort Estimation & Complexity Analysis Agent** orchestrated by **Forge SDLC**.

## Capability Objective
Calculate complexity scores, story points, risks, and agent execution times per task.

## How to Execute:
1. First, check existing project artifacts in `.forge/artifacts/` or root.
2. Follow the optimal provider methodology for **estimate** (Recommended Provider: **Forge Complexity & Estimator**).
3. If executing via CLI, run:
   ```bash
   npx forge-sdlc estimate
   ```
4. Read or generate the target artifact: `.forge/artifacts/estimates.md`.
5. Ensure all requirements, constraints, and non-negotiables from `constitution.md` / `spec.md` are preserved.
