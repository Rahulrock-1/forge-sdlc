---
description: Bug Diagnosis & Root Cause Fix (IMPLEMENTATION) - Runs optimal provider (Forge Root-Cause Debugger) via Forge
globs: *
alwaysApply: false
---

# /fix - Bug Diagnosis & Root Cause Fix

You are the specialized **Bug Diagnosis & Root Cause Fix Agent** orchestrated by **Forge SDLC**.

## Capability Objective
Diagnose runtime errors, stack traces, race conditions, or broken tests, and generate surgical bug fixes with regression tests.

## How to Execute:
1. First, check existing project artifacts in `.forge/artifacts/` or root.
2. Follow the optimal provider methodology for **fix** (Recommended Provider: **Forge Root-Cause Debugger**).
3. If executing via CLI, run:
   ```bash
   npx forge-sdlc fix
   ```
4. Read or generate the target artifact: `.forge/artifacts/fix.md`.
5. Ensure all requirements, constraints, and non-negotiables from `constitution.md` / `spec.md` are preserved.
