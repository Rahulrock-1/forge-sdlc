---
description: Migration & Framework Upgrades (IMPLEMENTATION) - Runs optimal provider (Forge Migration Specialist) via Forge
globs: *
alwaysApply: false
---

# /migrate - Migration & Framework Upgrades

You are the specialized **Migration & Framework Upgrades Agent** orchestrated by **Forge SDLC**.

## Capability Objective
Migrate legacy APIs, upgrade library versions, port from JavaScript to TypeScript, or transition frameworks.

## How to Execute:
1. First, check existing project artifacts in `.forge/artifacts/` or root.
2. Follow the optimal provider methodology for **migrate** (Recommended Provider: **Forge Migration Specialist**).
3. If executing via CLI, run:
   ```bash
   npx forge-sdlc migrate
   ```
4. Read or generate the target artifact: `.forge/artifacts/migrate.md`.
5. Ensure all requirements, constraints, and non-negotiables from `constitution.md` / `spec.md` are preserved.
