---
description: Multi-Provider Swarm Consensus Agent - Executes BMAD, Spec Kit, and Internal in parallel and synthesizes consensus in swarm-<cap>.md
---

Execute Forge capability: **Multi-Provider Swarm Consensus (/swarm)**

## 📥 Required Context:
- Target Capability: `review`, `security`, `analyze`, or `architecture`

## 🐝 Instructions:
1. Run target capability across BMAD, Spec Kit, and Internal concurrently.
2. Synthesize multi-perspective consensus into `.forge/artifacts/swarm-<capability>.md`.
3. Run: `npx forge-sdlc swarm review` or `npx forge-sdlc swarm security`.
