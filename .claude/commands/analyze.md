---
description: Perform deep semantic audit across artifacts (constitution -> spec -> architecture -> plan -> tasks -> code) to detect drift, gaps, or contradictions.
---

Execute Forge capability: **Cross-Artifact Consistency Analysis**
Run: `npx forge-sdlc analyze` and synthesize target artifact `analysis.md`.
